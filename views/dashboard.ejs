<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>🌾 AgroBot Dashboard</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    /* 🌿 Reset & Base */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Poppins", sans-serif;
    }

    body {
      background: linear-gradient(135deg, #e8f5e9, #f1f8e9);
      color: #333;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    main.container {
      width: 90%;
      max-width: 1100px;
      margin: 3rem auto;
      text-align: center;
      padding: 2rem;
      background: #ffffffa8;
      border-radius: 20px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
      backdrop-filter: blur(8px);
    }

    h1 {
      font-size: 2.5rem;
      color: #2e7d32;
      margin-bottom: 2rem;
      text-shadow: 1px 1px 2px rgba(46, 125, 50, 0.2);
    }

    /* 🌾 Cards Layout */
    .cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1.5rem;
      margin-bottom: 2rem;
    }

    .card {
      background: linear-gradient(145deg, #e8f5e9, #c8e6c9);
      border-radius: 15px;
      padding: 1.5rem;
      transition: all 0.3s ease;
      box-shadow: 0 5px 15px rgba(76, 175, 80, 0.15);
    }

    .card:hover {
      transform: translateY(-8px);
      box-shadow: 0 10px 25px rgba(76, 175, 80, 0.25);
    }

    .card h3 {
      color: #388e3c;
      font-size: 1.2rem;
      margin-bottom: 0.5rem;
    }

    .card p {
      font-size: 1.5rem;
      font-weight: 600;
      color: #1b5e20;
    }

    /* 🌻 Update Time */
    .update {
      margin: 1rem 0 2rem;
      font-size: 0.95rem;
      color: #4e4e4e;
      background: #f1f8e9;
      display: inline-block;
      padding: 0.6rem 1rem;
      border-radius: 10px;
    }

    /* 🌾 Buttons Section */
    .controls {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 1.2rem;
    }

    .controls form {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 1rem;
    }

    .btn {
      border: none;
      outline: none;
      padding: 0.8rem 1.5rem;
      border-radius: 50px;
      font-size: 1rem;
      cursor: pointer;
      transition: 0.3s;
      color: #fff;
      font-weight: 600;
      display: flex;
      align-items: center;
      gap: 0.4rem;
    }

    .btn.refresh {
      background: #81c784;
    }

    .btn.water {
      background: #4fc3f7;
    }

    .btn.scan {
      background: #ba68c8;
    }

    .btn.detect {
      background: #ffb74d;
    }

    .btn.stop {
      background: #e57373;
    }

    .btn:hover {
      transform: translateY(-4px);
      opacity: 0.9;
    }

    footer {
      text-align: center;
      margin-top: auto;
      padding: 1rem 0;
      background: #2e7d32;
      color: #fff;
      font-size: 0.9rem;
      letter-spacing: 0.5px;
    }

    @media (max-width: 600px) {
      h1 { font-size: 1.8rem; }
      .card p { font-size: 1.2rem; }
      .btn { font-size: 0.9rem; padding: 0.7rem 1rem; }
    }
  </style>
</head>

<body>
  <%- include("partials/header") %>

  <main class="container">
    <h1>🌱 AgroBot Farm Dashboard</h1>

    <div class="cards">
      <div class="card"><h3>Soil Moisture</h3><p><%= data.moisture %>%</p></div>
      <div class="card"><h3>Soil Density</h3><p><%= data.density %> g/cm³</p></div>
      <div class="card"><h3>pH Level</h3><p><%= data.ph %></p></div>
      <div class="card"><h3>Nitrogen</h3><p><%= data.nitrogen %> mg/kg</p></div>
      <div class="card"><h3>Disease</h3><p><%= data.disease %></p></div>
    </div>

    <p class="update">Last Updated: <%= data.updatedAt %></p>

    <div class="controls">
      <form action="/refresh" method="get">
        <button type="submit" class="btn refresh">🔄 Refresh Data</button>
      </form>

      <form action="/control" method="post">
        <button name="action" value="water" class="btn water">💧 Start Watering</button>
        <button name="action" value="scan" class="btn scan">🧪 Scan Soil</button>
        <button name="action" value="detect" class="btn detect">📸 Detect Disease</button>
        <button name="action" value="stop" class="btn stop">⏹ Stop Bot</button>
      </form>
    </div>
  </main>

  <%- include("partials/footer") %>
</body>
</html>
